# Publish status

GitHub Pages deployment is configured from `main` through `.github/workflows/deploy-pages.yml`.

The repository Pages source is set to **GitHub Actions**. The public artifact contains only learner-facing static files and Case 001 learner data. Internal governance, build documentation and QA files are excluded from the deployed artifact.

Clinical release remains **HOLD**.
